name: Ibexa Reusable / Cross Org PR

on:
  push:
    branches-ignore:
      - "temp_*"

jobs:
  call:
    uses: ibexa/gh-workflows/.github/workflows/cross-merge.yml@ibx-11778-update-deprecated-actions
    secrets:
      robot-token: ${{ secrets.EZROBOT_PAT }}
